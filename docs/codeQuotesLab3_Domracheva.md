# Блоки кода
Встроенный (inline) код:
Команда `git status` показывает состояние репозитория.
Многострочный код:
```python
print("Hello, Markdown!")
```
Можно указать язык: python, js, bash, csharp и др.
# Цитаты
Цитата создаётся символом >:
> Это цитата в Markdown.
Вложенная цитата:
> Первый уровень
>> Второй уровень


# примеры команды Git в виде inline-кода:
`git add .`
`git commit -m "message"`
`git push`

# Пример на языке Python:

```python
def greet(name):
    print(f"Hello, {name}!")

greet("Markdown")
```
# Блок кода без указания языка

```
Блок кода без указания языка.
```

# Цитаты (обычная и вложенная)
>Обычная цитата 

>Вложенная
>>цитата.